name: dsi
description: Data Sync Ineterface - a tiny and powerfull state manager.
version: 1.0.1
repository: https://github.com/hacp0012/dsi

environment:
  sdk: ^3.9.2

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0
  flutter:
    sdk: flutter

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

# This package supports all platforms listed below.
platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

topics:
  - state-manager
  - state
  - getit
  - watch
  - ui
